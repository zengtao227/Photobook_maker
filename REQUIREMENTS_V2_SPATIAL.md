# Photobook Pro - V2 未来版需求文档 (Spatial Edition)

> **核心理念**: 自由、无限、沉浸。
>
> V2 版本是颠覆性的创新，它打破了传统软件的边界。它不再是一个 "编辑器"，而是一个 "无限创作白板"。这里的核心交互逻辑是 **"空间 (Spatial)"** 和 **"对象 (Object)"**。

## 1. 产品定位与视觉风格

### 1.1 无限空间 UI (Infinite Spatial UI)
*   **无限画布**: 没有 "画布尺寸" 的限制。屏幕就是一个无限大的桌子。
*   **视口操作**:
    *   按住空格/中键拖动 = 平移视角 (Pan)。
    *   滚轮/双指捏合 = 无级缩放 (Zoom)，从 "鸟瞰全书" 到 "像素级调整"。
*   **非线性创作**: 用户可以将几百张照片先 "撒" 在桌面上，进行粗略的分类、堆叠，然后再一张张挑选放进书里。

### 1.2 悬浮界面 (Floating UI)
为了最大化创作空间，摒弃固定的侧边栏。
*   **岛屿式菜单 (Island Menu)**: 顶部悬浮的长条胶囊菜单，包含全局控制。
*   **工具箱 (Toolbox)**: 悬浮在左侧或跟随鼠标的小型工具条。
*   **上下文菜单 (Contextual HUD)**: 当你选中一张照片时，编辑选项（裁剪、滤镜）直接悬浮在照片旁边，鼠标不需要长距离移动。

### 1.3 视觉风格：SaaS 2.0 / Figma 风
*   **背景**: 浅灰色无限点阵网格 (Dot Grid)。
*   **UI 元素**: 极高的圆角、细腻的微阴影、模糊半透明背景。
*   **物理动效**: 照片在拖动时要有惯性，放置时要有轻微的弹跳感。

---

## 2. 核心创新功能 (Innovation Features)

### 2.1 空间整理 (Spatial Organization)
*   **自由堆叠**: 照片可以像扑克牌一样随意堆叠在桌面上。
*   **自动整理 (Auto-Arrange)**: 提供 "整理桌面" 按钮，一键将散落的照片按颜色或时间排列成整齐的矩阵。

### 2.2 拖拽即创造
*   **跨页流转**: 照片可以从左页直接拖过中缝到右页，或者横跨两页。
*   **桌面对接**: 从桌面上（画布非打印区）把照片拖入书页区域，书页会自动高亮显示 "可放置区域"。

### 2.3 沉浸式预览
*   **3D 翻书预览**: 点击预览时，相机视角拉近，背景变暗，书本呈现真实的 3D 翻页动画。

### 2.4 国际化 (i18n)
*   **策略**: 继承 V1 的多语言架构，确保所有新增的创新功能（如HUD菜单、悬浮提示）均完全支持双语。
*   **默认**: 英文。
*   **可选**: 简体中文。
*   **实现**: 使用 String Catalogs 统一管理 V1 和 V2 的文案资源。

---

## 3. 技术挑战与选型 (Tech Stack)

相比 V1，V2 对图形性能要求极高。

*   **核心渲染引擎**:
    *   **方案 A (推荐)**: **Metal** 或 **SpriteKit**。SwiftUI 的标准视图可能无法支撑成百上千张高分辨率照片的平滑缩放和拖拽。需要接近游戏引擎的渲染能力。
    *   **方案 B**: 高度优化的 SwiftUI `Canvas` + `GeometryReader`，但需要极其精细的内存管理（这是 Figma 的 WebGL 技术路线的 Native 复刻版）。
*   **手势交互**: 需要重写这就即使系统标准手势（平移、缩放、旋转），使用 `UIGestureRecognizer` 的底层实现以确保跟手度。

---

## 4. 开发优先级 (Roadmap)

1.  **Phase 1 (引擎研发)**: 构建一个高性能的 "无限缩放网格" 引擎，确保放置 100 张图片缩放不掉帧。
2.  **Phase 2 (交互系统)**: 实现核心的 "选择系统"（框选、多选）和 "变换系统"（拖拽、缩放）。
3.  **Phase 3 (容器逻辑)**: 实现 "书页" 作为一个特殊的容器，能够吸附和容纳照片对象。
4.  **Phase 4**: 迁移 V1 的数据结构，使其兼容这种新的视图模式。

---

## 5. V1 与 V2 的关系

*   **数据互通**: 两者应使用相同的底层数据模型 (`.photobook` 项目文件)。
*   **渐进式升级**: V1 是 "编辑模式 (Edit Mode)"，V2 可以作为未来的 "白板模式 (Whiteboard Mode)" 引入，用户可以在两者间切换。

