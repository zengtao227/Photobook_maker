
   - 重启前确保已手动保存

---

**最后更新**: 2026年1月8日  
**版本**: v1.0
- 寻找 "✅ Saved" 或 "❌ Failed" 提示

2. 检查文件权限
   - 确保应用有权限访问 `~/Library/Application Support/`
   - 检查项目文件夹是否可写

3. 重启应用
   - 有时重启可以解决临时问题的项目名称
   - 包含日期或版本号
   - 例如："家庭相册_2026春节"

---

## 📞 需要帮助？

如果遇到保存相关的问题：

1. 查看控制台日志
   - 打开应用后查看终端输出
   
   - 不要随意移动或删除已导入的照片
   - 考虑使用照片管理软件

4. **项目命名规范**
   - 使用有意义集中存放在固定文件夹
   - 将照片**
   - 每周备份一次项目文件夹
   - 重要项目完成后立即备份

3. **妥善管理照片文件**✅ 照片库
- 所有导入的照片列表
- 照片的文件路径
- 照片的元数据（拍摄日期、尺寸等）

### ❌ 不保存的内容
- 原始照片文件（只保存路径引用）
- 缩略图（每次打开时重新生成）
- 临时编辑状态

---

## 🎯 最佳实践

1. **养成手动保存的习惯**
   - 完成一个页面后按 ⌘S
   - 添加重要照片后按 ⌘S
   - 准备休息前按 ⌘S

2. **定期备份项目文件**不建议手动编辑**，可能导致文件损坏

---

## 📊 保存内容清单

每次保存会包含以下内容：

### ✅ 书籍设置
- 页面尺寸（A4、A5、A6等）
- 装订方式（软皮装、精装、骑马钉等）
- 书脊宽度
- 总页数

### ✅ 所有页面
- 封面（外面）
- 封底（外面）
- 所有内页跨页

### ✅ 每个页面的图层
- 照片图层：位置、大小、旋转、裁剪、滤镜、边框、阴影
- 文字图层：内容、字体、大小、颜色、对齐方式
- 贴纸图层：位置、大小、旋转

###  查看控制台是否有 "✅ Saved project" 提示

### Q: 自动保存会不会太频繁，影响性能？

**A:** 不会。系统使用了节流机制：
- 只在有修改时才触发
- 2秒内的多次修改只保存一次
- 保存操作非常快速（JSON格式）

### Q: 我可以关闭自动保存吗？

**A:** 目前不支持关闭自动保存，因为：
- 自动保存是防止数据丢失的重要功能
- 节流机制确保不会影响性能
- 你仍然可以使用手动保存（⌘S）

### Q: 项目文件可以用文本编辑器打开吗？

**A:** 可以！项目文件是格式化的JSON：
- 可以用任何文本编辑器查看
- 可以看到所有页面、图层、设置
-  我修改了图片位置，但关闭后再打开位置变了？

**A:** 这个问题已在最新版本修复。现在系统会：
- 保存完整的书籍结构（所有页面）
- 保存每个图层的精确位置、大小、旋转角度
- 自动保存会在2秒后触发

如果仍有问题，请：
1. 修改后手动按 ⌘S 保存
2. 等待2秒确保自动保存完成
3.整个 `Projects` 文件夹到外部硬盘
- 或使用 Time Machine 等备份工具
- 防止意外数据丢失

### 3. 照片文件管理

- 项目文件只保存照片的**路径引用**
- 不要移动或删除已导入的照片文件
- 如果照片文件被移动，项目中会显示为缺失

---

## 🐛 常见问题

### Q:1. 自动保存的限制

虽然有自动保存，但建议：
- 完成重要编辑后，**手动保存一次**（⌘S）
- 这样可以立即确认保存成功
- 不依赖2秒的延迟

### 2. 项目文件备份

建议定期备份项目文件：
- 复制
   - 重启应用或刷新项目列表

3. **注意事项**
   - 项目文件包含所有页面布局和设置
   - 但**不包含**原始照片文件
   - 需要确保照片文件在新电脑上的路径相同，或重新导入照片

---

## ⚠️ 重要提示

### 

1. **导出项目**
   - 在项目浏览器点击"项目文件夹"
   - 找到你的项目文件（如 `project-abc123.json`）
   - 复制该文件到U盘或云盘

2. **导入项目**
   - 在另一台电脑上打开 PhotobookApp
   - 点击"项目文件夹"打开项目目录
   - 将复制的项目文件粘贴到这个文件夹

### 项目文件格式

- 文件格式：JSON（纯文本格式）
- 文件名：`project-{UUID}.json`
- 可读性：使用了格式化输出，可以用文本编辑器查看

---

## 🔄 项目转移

### 如何在不同电脑间转移项目？s/
```

### 如何查看项目文件？

1. **在项目浏览器中**
   - 点击右上角的"项目文件夹"按钮
   - 会自动打开项目存储文件夹

2. **在项目卡片上**
   - 右键点击任何项目卡片
   - 选择"在Finder中显示"
   - 会直接定位到该项目文件
---

## 💾 项目文件存储位置

### 项目保存在哪里？

所有项目文件都保存在：
```
~/Library/Application Support/PhotobookPro/Project修改

3. **关闭项目时自动保存**
   - 点击"返回项目列表"时
   - 退出应用时
   - 系统会自动保存所有修改

### 自动保存的优点

- ✅ 不用担心忘记保存
- ✅ 防止意外丢失工作
- ✅ 可以随时关闭应用，下次打开继续编辑

   - 系统会在 **2秒后** 自动保存
   - 你会在控制台看到 "🔄 Auto-saved project" 的提示

2. **切换页面时自动保存**
   - 从封面切换到内页时
   - 从一个跨页切换到另一个跨页时
   - 系统会自动保存当前页面的pp 会在以下情况自动保存：

1. **定时自动保存**
   - 每当你修改项目（添加图片、调整位置、编辑文字等） 准备关闭应用前
- 想要确保当前工作已保存时
- 准备切换到其他项目前

---

## 🔄 自动保存

### 自动保存如何工作？

PhotobookA+ S）
   - 这是最快的保存方式

### 什么时候需要手动保存？

- 完成重要编辑后
-nd `（Comma
---

## 📝 手动保存

### 如何手动保存？

有两种方式可以手动保存项目：

1. **点击保存按钮**
   - 在工具栏右上角找到"保存"按钮（带有下载图标）
   - 点击即可立即保存当前项目

2. **使用键盘快捷键**
   - 按 `⌘S# PhotobookApp 保存功能使用指南

## 保存功能说明

PhotobookApp 提供了两种保存方式：**手动保存**和**自动保存**。
